# API contract

The project includes a prepared SAP OData client for future integration. The interface supports methods for listing, reading, creating, updating, and executing an action against invoice records.
