# Clean Core approach

The project follows Clean Core principles by keeping custom logic in a dedicated namespace. It does not modify SAP standard objects and uses side-by-side extensions. Where possible, released APIs and well-defined extension points are preferred over custom modifications to the core system.
